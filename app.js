const state={hive:null,weather:null,charts:[],co2Chart:null,output:[],metrics:{temp:{color:'#e06b3c',title:'溫度 (°C)'},humidity:{color:'#2878b8',title:'相對濕度 (%)'},co2:{color:'#cf493d',title:'CO₂ (ppm)'}}};
const keys=['temp','humidity','co2'],aliases={time:['timestamp','datetime','date time','日期時間','時間','日期','date','time','系統記錄時間'],temp:['temperature','temp','溫度','temperature (°c)','temperature_c','氣溫'],humidity:['humidity','rh','濕度','humidity (%)','relative humidity'],co2:['co2','carbon dioxide','二氧化碳','co₂','co2(ppm)','co2 ppm']};const $=id=>document.getElementById(id),norm=s=>String(s??'').trim().toLowerCase().replace(/[\s_\-()]/g,''),esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),label=k=>({time:'日期／時間 *',temp:'溫度',humidity:'相對濕度',co2:'CO₂'})[k];
function find(h,k){return h.find(x=>aliases[k].some(a=>norm(x)===norm(a)||norm(x).includes(norm(a))))||''}function decode(b){const c=$('csvEncoding').value,u=new TextDecoder('utf-8').decode(b);return c==='utf-8'?u:c==='big5'?new TextDecoder('big5').decode(b):u.includes('�')?new TextDecoder('big5').decode(b):u}async function read(file){const b=await file.arrayBuffer(),wb=/\.csv$/i.test(file.name)?XLSX.read(decode(b),{type:'string'}):XLSX.read(b,{type:'array',cellDates:true}),rows=XLSX.utils.sheet_to_json(wb.Sheets[wb.SheetNames[0]],{defval:'',raw:false});if(!rows.length)throw Error(`${file.name} 找不到資料列`);return rows}
async function load(files,source){try{const rows=(await Promise.all([...files].map(read))).flat();state[source]={rows,headers:[...new Set(rows.flatMap(Object.keys))],mapping:{},format:'auto'};setup();$(`${source}Status`).textContent=`已載入 ${source==='weather'?`${files.length} 個檔案，`:files[0].name+' · '}${rows.length.toLocaleString()} 筆資料`}catch(e){$(`${source}Status`).textContent=`無法讀取：${e.message}`}}$('hiveFile').onchange=e=>e.target.files.length&&load(e.target.files,'hive');$('weatherFile').onchange=e=>e.target.files.length&&load(e.target.files,'weather');
function setup(){if(!state.hive&&!state.weather)return;$('mappingCard').classList.remove('hidden');$('controlsCard').classList.remove('hidden');let html='';for(const source of ['hive','weather']){const d=state[source];if(!d)continue;html+=`<div class="mapping-group"><h3>${source==='hive'?'蜂箱 SCD30':'外部氣象站（已合併多檔）'}</h3>`;for(const k of ['time',...keys]){d.mapping[k]=d.mapping[k]||find(d.headers,k);html+=`<label>${label(k)}<select data-source="${source}" data-key="${k}"><option value="">不使用</option>${d.headers.map(h=>`<option value="${esc(h)}" ${h===d.mapping[k]?'selected':''}>${esc(h)}</option>`).join('')}</select></label>`}html+=`<label>日期格式<select data-format="${source}"><option value="auto" ${d.format==='auto'?'selected':''}>自動辨識</option><option value="ymd" ${d.format==='ymd'?'selected':''}>年／月／日（2026/07/04）</option><option value="dmy" ${d.format==='dmy'?'selected':''}>日／月／年（04/07/2026）</option><option value="mdy" ${d.format==='mdy'?'selected':''}>月／日／年（07/04/2026）</option></select></label></div>`}$('mappingGrid').innerHTML=html;document.querySelectorAll('[data-source]').forEach(e=>e.onchange=()=>{state[e.dataset.source].mapping[e.dataset.key]=e.value;setDates()});document.querySelectorAll('[data-format]').forEach(e=>e.onchange=()=>{state[e.dataset.format].format=e.value;setDates()});setDates()}
function parse(v,fmt='auto'){if(v instanceof Date&&!isNaN(v))return v;if(typeof v==='number'&&v>20000&&v<70000)return new Date((v-25569)*86400000);const str=String(v).trim(),m=str.match(/(\d{1,4})\D(\d{1,2})\D(\d{1,4})(?:\D+(\d{1,2}):?(\d{2})?:?(\d{2})?)?/);if(m){let[a,b,c]=[+m[1],+m[2],+m[3]],y,mo,da;if(fmt==='dmy'){[y,mo,da]=[c,b,a]}else if(fmt==='mdy'){[y,mo,da]=[c,a,b]}else if(a>999||fmt==='ymd'){[y,mo,da]=[a,b,c]}else{const native=new Date(str);if(!isNaN(native))return native;[y,mo,da]=[c,b,a]}return new Date(y,mo-1,da,+(m[4]||0),+(m[5]||0),+(m[6]||0))}const native=new Date(str);return isNaN(native)?null:native}function datesOf(d){return d?.mapping.time?d.rows.map(r=>parse(r[d.mapping.time],d.format)).filter(Boolean):[]}function local(d){const z=n=>String(n).padStart(2,'0');return `${d.getFullYear()}-${z(d.getMonth()+1)}-${z(d.getDate())}T${z(d.getHours())}:${z(d.getMinutes())}`}
function setDates(){const hive=datesOf(state.hive).sort((a,b)=>a-b),all=[...hive,...datesOf(state.weather)].sort((a,b)=>a-b),base=hive.length?hive:all;if(!base.length)return;$('startDate').value=local(base[0]);$('endDate').value=local(base.at(-1));$('dataRange').textContent=`SCD30 資料期間：${base[0].toLocaleString('zh-TW')} 至 ${base.at(-1).toLocaleString('zh-TW')}`}
function bucket(t,m){const d=new Date(t);if(m==='raw')return +d;if(m==='5min')d.setMinutes(Math.floor(d.getMinutes()/5)*5,0,0);if(m==='15min')d.setMinutes(Math.floor(d.getMinutes()/15)*15,0,0);if(m==='hour')d.setMinutes(0,0,0);if(m==='day')d.setHours(0,0,0,0);return +d}function keep(k,v){const min=$(`${k}Min`).value,max=$(`${k}Max`).value;return (!min||v>=Number(min))&&(!max||v<=Number(max))}
function calculate(){const start=+new Date($('startDate').value),end=+new Date($('endDate').value),mode=$('aggregation').value,raw=[];for(const source of ['hive','weather']){const d=state[source];if(!d?.mapping.time)continue;for(const row of d.rows){const time=parse(row[d.mapping.time],d.format);if(!time||time<start||time>end)continue;for(const key of keys){const col=d.mapping[key],value=Number(String(row[col]??'').replace(',','.'));if(col&&Number.isFinite(value)&&keep(key,value))raw.push({source,key,time:bucket(time,mode),value,original:+time})}}}const g=new Map;raw.forEach(x=>{const id=`${x.source}|${x.key}|${x.time}`,v=g.get(id)||{...x,sum:0,n:0};v.sum+=x.value;v.n++;g.set(id,v)});state.output=[...g.values()].map(v=>({...v,value:v.sum/v.n}));return raw.length}
function controls(){return keys.map(k=>`<label class="metric"><strong>${label(k)}</strong><input data-metric="${k}" data-prop="color" type="color" value="${state.metrics[k].color}"/><input data-metric="${k}" data-prop="title" type="text" value="${esc(state.metrics[k].title)}"/></label>`).join('')}function dataFor(k,source){return state.output.filter(x=>x.key===k&&x.source===source).map(x=>({x:x.time,y:x.value}))}function xScale(){return{type:'linear',ticks:{callback:v=>new Date(v).toLocaleString('zh-TW',{month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit'})},title:{display:true,text:'日期與時間'}}}
function makeSets(k){return['hive','weather'].filter(s=>dataFor(k,s).length).map(s=>({label:s==='hive'?'蜂箱 SCD30':'外部氣象站',data:dataFor(k,s),borderColor:state.metrics[k].color,backgroundColor:state.metrics[k].color,borderWidth:2,pointRadius:0,borderDash:s==='weather'?[7,4]:[],tension:.12,spanGaps:$('missing').value==='connect',yAxisID:k}))}function opts(scales){return{responsive:true,maintainAspectRatio:false,interaction:{mode:'index',intersect:false},plugins:{legend:{position:'bottom'}},scales}}
function draw(){state.charts.forEach(c=>c.destroy());state.charts=[];if(state.co2Chart)state.co2Chart.destroy();const present=keys.filter(k=>state.output.some(x=>x.key===k)),mode=$('chartMode').value,span=+new Date($('endDate').value)- +new Date($('startDate').value);if(mode==='combined'){$('charts').innerHTML='<div class="chart-card"><h3>溫度、相對濕度與 CO₂ 合併圖</h3><canvas id="combined"></canvas></div>';const axes={x:xScale()};present.forEach((k,i)=>axes[k]={position:i?'right':'left',offset:i===2,title:{display:true,text:state.metrics[k].title},grid:{drawOnChartArea:i===0}});state.charts.push(new Chart($('combined'),{type:'line',data:{datasets:present.flatMap(makeSets)},options:opts(axes)}))}else{let groups=present;if(mode==='daily'){const days=[...new Set(state.output.map(x=>new Date(x.time).toDateString()))];groups=days.flatMap(day=>present.map(k=>`${day}|${k}`))}$('charts').innerHTML=groups.map(x=>{const [day,k]=x.includes('|')?x.split('|'):[null,x];return `<div class="chart-card"><h3>${day?new Date(day).toLocaleDateString('zh-TW')+' · ':''}${esc(state.metrics[k].title)}</h3><canvas id="chart-${groups.indexOf(x)}"></canvas></div>`}).join('');groups.forEach((x,i)=>{const [day,k]=x.includes('|')?x.split('|'):[null,x],sets=makeSets(k).map(s=>day?{...s,data:s.data.filter(p=>new Date(p.x).toDateString()===day)}:s);state.charts.push(new Chart($(`chart-${i}`),{type:'line',data:{datasets:sets},options:opts({x:xScale(),[k]:{title:{display:true,text:state.metrics[k].title}}})}))})}drawCO2Daily()}
function drawCO2Daily(){const rows=state.output.filter(x=>x.source==='hive'&&x.key==='co2'),card=$('co2DailyCard');if(!rows.length){card.classList.add('hidden');return}card.classList.remove('hidden');const bins=Array.from({length:24},()=>({sum:0,n:0}));rows.forEach(x=>{const b=bins[new Date(x.time).getHours()];b.sum+=x.value;b.n++});state.co2Chart=new Chart($('co2DailyChart'),{type:'line',data:{labels:bins.map((_,i)=>`${String(i).padStart(2,'0')}:00`),datasets:[{label:'蜂箱 SCD30 CO₂ 平均',data:bins.map(b=>b.n?b.sum/b.n:null),borderColor:state.metrics.co2.color,backgroundColor:state.metrics.co2.color,borderWidth:2,pointRadius:2,tension:.12,spanGaps:false}]},options:{responsive:true,maintainAspectRatio:false,scales:{x:{title:{display:true,text:'每日時間'}},y:{title:{display:true,text:state.metrics.co2.title}}}}})}
function render(){const n=calculate();if(!n)return alert('請確認日期／時間欄位、日期格式及欲分析的欄位／篩選條件。');$('chartCard').classList.remove('hidden');$('metricControls').innerHTML=controls();document.querySelectorAll('[data-metric]').forEach(e=>e.oninput=x=>{state.metrics[x.target.dataset.metric][x.target.dataset.prop]=x.target.value;draw()});draw();const ts=new Set(state.output.map(x=>x.time));$('qualitySummary').innerHTML=`<div class="quality-item"><strong>${n.toLocaleString()}</strong><span>有效原始數值</span></div><div class="quality-item"><strong>${state.output.length.toLocaleString()}</strong><span>彙整後觀測值</span></div><div class="quality-item"><strong>${ts.size.toLocaleString()}</strong><span>時間點</span></div><div class="quality-item"><strong>${$('aggregation').selectedOptions[0].text}</strong><span>目前彙整尺度</span></div>`}$('renderButton').onclick=render;
document.querySelectorAll('[data-range]').forEach(b=>b.onclick=()=>{const d=datesOf(state.hive).sort((a,b)=>a-b);if(!d.length)return;const end=d.at(-1),n=Number(b.dataset.range);$('endDate').value=local(end);$('startDate').value=local(b.dataset.range==='all'?d[0]:new Date(Math.max(+d[0],+end-n*86400000)))});$('downloadPng').onclick=()=>{const cs=[...document.querySelectorAll('#charts canvas'),$('co2DailyChart')].filter(Boolean),o=document.createElement('canvas'),w=Math.max(...cs.map(c=>c.width)),h=cs.reduce((n,c)=>n+c.height,0);o.width=w;o.height=h;const x=o.getContext('2d');x.fillStyle='white';x.fillRect(0,0,w,h);let y=0;cs.forEach(c=>{x.drawImage(c,0,y);y+=c.height});const a=document.createElement('a');a.href=o.toDataURL('image/png');a.download='hive-weather-timeseries.png';a.click()};$('downloadCsv').onclick=()=>{const r=['time,source,metric,value,samples',...state.output.map(x=>`${new Date(x.time).toISOString()},${x.source},${x.key},${x.value},${x.n}`)],a=document.createElement('a');a.href=URL.createObjectURL(new Blob(['\ufeff'+r.join('\n')],{type:'text/csv;charset=utf-8'}));a.download='hive-weather-aggregated.csv';a.click()};
